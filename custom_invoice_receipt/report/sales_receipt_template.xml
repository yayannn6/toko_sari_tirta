<odoo>
  <template id="report_receipt_sale">
      <t t-call="web.html_container">

        <script>
          window.onload = function() {
            window.print(); 
          }
        </script>

        <style>
          @font-face {
            font-family: 'LiberationSerifCustom';
            src: url('/custom_invoice_receipt/static/src/fonts/LiberationSerif-Regular.ttf') format('truetype');
            }
            .receipt-font {
              font-family: 'LiberationSerifCustom', serif;
            }
        </style>        

        <t t-foreach="docs" t-as="so">
          <t t-set="company" t-value="so.company_id"/>

          <!-- ⬇️ HANYA line-height dinaikkan -->
          <div class="receipt-font" style="width:210px; font-size:14px; line-height:1.35; font-weight:600;">

            <!-- HEADER COMPANY INFO -->
            <div style="text-align:center; margin-bottom:4px;">
              <t t-if="company.logo">
                <img t-att-src="'data:image/png;base64,%s' % company.logo.decode('utf-8')" width="55" style="margin-bottom:4px;"/>
              </t>

              <h5 style="margin:4px 0;font-weight:700;">
                <t t-esc="company.name"/>
              </h5>

              <p style="margin:4px 0;font-size:13px"><t t-esc="company.street or ''"/></p>
              <p style="margin:4px 0;font-size:13px"><t t-esc="company.phone or ''"/></p>
              <p style="margin:4px 0;font-size:13px"><t t-esc="company.website or ''"/></p>

              <p style="margin:4px 0;">---------------------------</p>
            </div>

            <!-- INFO -->
            <p style="margin:4px 0;font-size:13px"><b>No:</b> <t t-esc="so.name"/></p>
            <p style="margin:4px 0;font-size:13px"><b>Tanggal:</b> <t t-esc="so.date_order"/></p>
            <p style="margin:4px 0;font-size:13px"><b>Pelanggan:</b> <t t-esc="so.partner_id.name"/></p>

            <p style="margin:4px 0;">---------------------------</p>

            <!-- ITEM LIST -->
            <table style="width:100%; border:0; font-size:14px; line-height:1.1;">
              <t t-foreach="so.order_line" t-as="line">

                <tr>
                  <td colspan="3" style="padding:3px 0;">
                    <t t-esc="line.product_id.name"/>
                  </td>
                </tr>

                <tr>
                  <td style="padding:3px 0;">
                    <t t-esc="int(line.product_uom_qty)"/> x
                    <t t-esc="'Rp {:,.0f}'.format(line.price_unit)"/>
                  </td>

                  <!-- DISKON -->
                  <t t-if="line.discount_amount &gt; 0">
                    <td style="font-size:12px; color:#444; padding:3px 0;">
                      - <t t-esc="'{:,.0f}'.format(line.discount_amount)"/>
                    </td>
                  </t>
                  <t t-else="">
                    <td style="font-size:12px; padding:3px 0;"/>
                  </t>

                  <td style="text-align:right; padding:3px 0;font-size:14px">
                    <t t-esc="'Rp {:,.0f}'.format(line.price_subtotal)"/>
                  </td>
                </tr>
                <tr>
                  <td colspan="3" style="padding:5px 0;">
                  </td>
                </tr>

              </t>
            </table>

            <p style="margin:4px 0;">---------------------------</p>

            <!-- TOTAL -->
            <p style="margin:4px 0;font-size:14px">
              <b>Total:</b>
              <span style="float:right; font-weight:bold; white-space:nowrap; display:inline-block;">
                Rp <t t-esc="'{:,.0f}'.format(so.amount_total)"/>
              </span>
            </p>

            <p style="text-align:center; margin:4px 0;">---------------------------</p>

            <!-- FOOTER -->
            <div style="text-align:center;">
              <p style="margin:4px 0;font-size:13px">Terima kasih!</p>
              <p style="margin:4px 0;">---------------------------</p>
              <p style="margin:4px 0; font-size:13px;">
                Dicetak pada <t t-esc="so.create_date.strftime('%d-%m-%Y')"/>
              </p>
            </div>

          </div>
        </t>

      </t>
  </template>
</odoo>
